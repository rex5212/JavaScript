  (x) //vai se usado como exemplo, dei o nome de valor.

  const prompt = require('prompt-sync')({sigint:true}); //habilita o uso do prompt

  var x//Significa variavel que o valor pode mudar ao longo do codigo

  const y = 0//Significa constante que o valor não se altera ao longo docodigo

  let z// Se o valor for criado dentro do bloco so funcionar no mesmo

//Um bloco e oque esta entre chaves {}

  ; //Quebra linha, separa as intruçoês 
    
  (x),(x) //a virgula simplesmente avalia ambos.
   
    console.log() //faz com que oque esta escrito dentro dos parenteses seja colocado na tela, no vs e mostrado no terminal

   
 //Esses operadores querem disse:
    (x  >= x);           (x > x);         (x == x);         (x <= x);          (x < x);        (x != x);
// Maior ou igual        Maior que          igual a        Menor ou igual      Menor que        Diferente



 //Esses operadores Aritméticos querem disse:

    (x%x);     
    // Retorna o inteiro restante dadivisão dos dois valores(x).
    
    (++x);       
    // Adiciona + 1 ao seu valores(x)
    
    (--x);  
    // subtrai - 1 de seu valores(x)
    
    (-x);        
    // retrona a negaçao de seu valores(x)
   
    (+x);  
    // Tenta converter o valor(x) em um número, sempre que possível.
    
    (x**x);
    // Calcula a potencia
    
   true, false //São resultados logicos chamados de Booleanos. 

   delete(x) /*apaga um objeto, uma propriedade de um objeto ou um elemento no índice especificado de uma matriz,
    se a mesma não for especificado como uma variavel (var).*/ 

      (x>x && x>x );             (x>x || x>x);             (!x);
/*São expressoes logicas, que && ambos tem que se verdadeiro para o resultado se verdadeiro,
 || so uma afirmaçao necessita se verdadeiro para concidera verdadeiro,
 e ! faz  que o valor (logico = Booleanos) seja o contrario.
*/

  // Tipos Primitivos são numeros, texto (string), Booleanos e null ou undefined qualquer coisa que nao for isso e um objeto
 
  /* Objeto: Um objeto (isto é, um membro do tipo objeto) é um conjunto de propriedades ou valores que agrega diversos valores
sendo valores primitivos ou outros objetos, em que cada propriedade tem um nome e um valor permitindo armazenar e recuperar 
esses valores pelo nome;. */ 

// Em Js as funções aritmeticas são:

  (x + x) 
//para adição,

  (x - x) 
//para subtração,

  (x * x) 
//para multiplicação,

  (x / x)
//para divisão

  (2 % x) 
//para módulo (resto da divisão); 

    //Alem da funçao math. , que possui varios calculos
      //link para as funções math https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/Math

  // Para a Data e hora tem a contrução date()..
     //link para as funções date() https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/Date

 //em um texto (string) existe  funções para a mesma que nem a funçao math
    // site para  essas funções https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/String 



 /*Uma expressão de criação de objeto gera um novo objeto e chama uma função (denominada
    construtora) para inicializar as propriedades desse objeto. As expressões de criação de objeto são
    como as expressões de chamada, exceto que são prefixadas com a palavra-chave new.
  */
 

